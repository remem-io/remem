// shim.c — dummy source file to ensure xcodebuild generates CRemem.o artifact.
// Xcode's build system requires at least one source file in a C target to produce
// the output object file required during dependency linking.
