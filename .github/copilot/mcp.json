{
  "mcpServers": {
    "remem": {
      "args": [
        "mcp",
        "--project",
        "remem-project"
      ],
      "command": "remem",
      "env": {
        "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}",
        "GOOGLE_API_KEY": "${GOOGLE_API_KEY}",
        "OPENAI_API_KEY": "${OPENAI_API_KEY}"
      },
      "type": "stdio"
    }
  }
}
